# Install with: pip install -r requirements.txt

# Core Data Science Stack
numpy>=1.21.0
pandas>=1.3.0
polars>=0.18.0  # High-performance DataFrame library (2-10x faster than pandas for CSV I/O)
scipy>=1.7.0
matplotlib>=3.4.0
seaborn>=0.11.0

# Machine Learning
scikit-learn>=1.0.0
statsmodels>=0.12.0

# Deep Learning & GPU Computing
torch>=1.10.0
cupy>=9.0.0  # Requires NVIDIA CUDA toolkit

# NVIDIA Apex (install separately after torch is available):
# pip install -v --disable-pip-version-check --no-cache-dir --no-build-isolation --config-settings "--build-option=--cpp_ext" --config-settings "--build-option=--cuda_ext" git+https://github.com/NVIDIA/apex

# High-Performance Computing
numba>=0.56.0
joblib>=1.0.0

# Graph Theory
networkx>=2.6.0

# Outlier Detection
pyod>=0.9.0

# Progress Bars & UI
tqdm>=4.62.0

# System Monitoring
psutil>=5.8.0

# Optional: For better performance and additional features
# These may already be satisfied by the above packages
# but listing them explicitly for clarity

# Data format support (likely already satisfied by pandas)
# openpyxl>=3.0.0  # Excel file support
# xlsxwriter>=3.0.0  # Excel writing support

# Scientific computing extensions (likely satisfied by scipy/sklearn)
# threadpoolctl>=3.0.0  # Thread pool control for scientific libraries

# Note: Standard library modules are included in Python by default:
# - argparse, csv, os, sys, time, logging, math, json, pickle
# - pathlib, subprocess, shutil, datetime, warnings
# - typing, dataclasses, multiprocessing, threading